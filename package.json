{
  "name": "sonarqube-poc",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "ts-node src/index.ts",
    "test": "jest --coverage && sed -i -e 's/.*sonarqube-poc//' coverage/clover.xml && sed -i -e 's/.*sonarqube-poc//' coverage/lcov.info && sed -i -e 's/.*sonarqube-poc//' test-report.xml"
  },
  "author": "alex@ahm",
  "license": "ISC",
  "devDependencies": {
    "@types/hapi": "^18.0.2",
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.2",
    "jest": "^24.9.0",
    "jest-sonar-reporter": "^2.0.0",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "@hapi/hapi": "^18.3.2"
  }
}
